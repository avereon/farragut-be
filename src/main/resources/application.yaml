spring:
  application:
    name: Farragut
  main:
    banner-mode: LOG

server:
  port: 8060

logging:
  file:
    name: farragut.log
  level:
    # Start with only WARN messages
    root: INFO
    # Increase verbosity as needed
    org:
      avereon:
        farragut: INFO

